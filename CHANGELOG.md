# CHANGELOG

قالب: [Keep a Changelog](https://keepachangelog.com/). تاریخ‌ها نسبی‌اند چون پروژه پیش از انتشار است.
منبع دقیق، تاریخچه‌ی git است؛ این‌جا فقط نقاط عطفِ خوانا.

## [Unreleased]

### Added (v9 — گزارشِ تخفیف‌های اعمال‌شده)
- **بخشِ «تخفیف‌های اعمال‌شده» در گزارش‌های مدیریتی، به‌تفکیکِ نماینده و کالا.** تا حالا `discount_amount`ِ snapshotِ هر خطِ سفارش (که همان لحظه‌ی تأیید از پله‌ی تخفیفِ حجمی نوشته می‌شود) هیچ‌جا جمع‌بندی نمی‌شد. حالا برای هر بازه: به‌ازای هر نماینده جمعِ مبلغِ تخفیف + تعدادِ خطِ تخفیف‌دار + درصدِ تخفیف نسبت به مبلغِ ناخالصِ همان نماینده، و همان جمع به‌تفکیکِ کالا (کدام کالا بیشترین تخفیف را گرفته).
  - عمداً **مستقل از منبعِ قیمت** است (لیست یا استثنای نمایندگی‌محورِ v9) — چون پله‌ی تخفیفِ حجمی رویِ هر دو منبع یکسان اعمال می‌شود؛ قاطی‌کردنِ این دو بحث یک عددِ گمراه‌کننده می‌ساخت.
  - `db/reports.ts::buildReports` دو آرایه‌ی تازه برمی‌گرداند (`discountsByAgent`, `discountsByProduct`)؛ فقط ردیف‌هایی که واقعاً تخفیف گرفته‌اند نشان داده می‌شوند (`HAVING SUM(discount_amount) > 0`). هر دو در خروجیِ اکسلِ گزارش‌ها هم به‌عنوانِ شیتِ جدا اضافه شدند.

### Fixed
- **🔴 `lib/date.ts`: تبدیلِ تاریخِ شمسی به رشته‌ی DATE (`YYYY-MM-DD`) یک روز عقب می‌افتاد.** حین ساختِ فیچرِ استثنای قیمتِ نمایندگی پیدا شد: `jalaliToDate(...).toISOString().slice(0,10)` برای TIMESTAMPTZ درست است (چون `jalaliToDate` عمداً نیمه‌شبِ **تهران** را برمی‌گرداند، نه UTC)، ولی همان الگو برای یک ستونِ DATEِ ساده یک روزِ گرگوریِ اشتباه می‌داد — کاربر «۸ مرداد» انتخاب می‌کرد و در دیتابیس «۷ مرداد» ذخیره می‌شد. تابعِ تازه‌ی `jalaliToIsoDate` این تبدیل را درست انجام می‌دهد (offsetِ ۳٫۵ساعته را برمی‌گرداند)؛ گزارش‌ها/مشتریان که با TIMESTAMPTZ کار می‌کنند دست‌نخورده ماندند چون برایشان همان الگوی قبلی درست بود.

### Added (v9 — استثنای قیمتِ نمایندگی‌محور از UI)
- **`/staff/agents` (تبِ نمایندگی‌ها) کارتِ هر نماینده «استثنای قیمتِ این نماینده» گرفت.** `agent_price_override` (بالاترین اولویتِ قیمت — حتی بر لیستِ اختصاصیِ نماینده هم ارجح است) تا حالا فقط با SQL قابلِ ساخت/دیدن بود. حالا فهرست + افزودن/ویرایش/حذف، با بازه‌ی اعتبارِ اختیاری (شروع/پایان، هرکدام می‌تواند نامحدود بماند).
  - **گاردِ هم‌پوشی:** ساختن یا ویرایشِ استثنایی که بازه‌اش با استثنای دیگرِ همان (نماینده،کالا) هم‌پوشی کند رد می‌شود (۴۰۹) — بدونِ این گارد، `resolvePricesIn` بینِ دو ردیفِ هم‌زمان معتبر فقط `rank` می‌بیند و انتخابِ غیرقطعی می‌کند (نه اینکه کدام تازه‌تر یا مشخص‌تر است).
  - `db/pricing.ts`: `listAgentOverrides` + `create/update/deleteAgentOverride`. gate: `authorizeAdmin` (همان گیتِ بقیه‌ی `/staff/agents`، نه pageKeyِ `prices` — چون خودِ صفحه‌ی نمایندگی‌ها هم admin-only است).
- **`/staff/catalog` تبِ «پله‌های تخفیفِ حجمی».** تا حالا `volume_discount` فقط با SQL قابلِ ساخت/دیدن بود — حتی خودِ GETِ پنلِ قیمت‌گذاری این پله‌ها را می‌گرفت ولی هیچ‌جا نشانشان نمی‌داد؛ کاملاً نامرئی. حالا فهرستِ کامل + افزودن/ویرایش/حذف از UI.
  - **نکته‌ی UX که صریح روی صفحه نوشته شده:** پله‌ها جمع نمی‌شوند — موتورِ قیمت (`resolvePricesIn`) از بینِ همه‌ی پله‌های واجدشرایط فقط **بیشترین درصد** را برمی‌دارد، نه مجموع. بدونِ این توضیح، کسی که با منطقِ «هرچه بیشتر بخری، تخفیفِ قبلی هم روش می‌ماند» می‌آید گیج می‌شود — این دقیقاً همان چیزی بود که وقتی فقط SQL بود هیچ‌کس توضیحش نمی‌داد.
  - پله‌ها گروه‌بندی‌شده نمایش داده می‌شوند (سبد → کالا → حداقلِ کارتنِ صعودی)، دقیقاً همان ترتیبی که موتورِ قیمت می‌بیند. «همه‌ی سبدها»/«همه‌ی کالاها» برای `price_list_id`/`variant_id` تهی.
  - `db/pricing.ts`: `listVolumeDiscounts` + `createVolumeDiscount`/`updateVolumeDiscount`/`deleteVolumeDiscount`. تکراری (همان سبد+کالا+حداقل) با یک SELECTِ قبل از INSERT رد می‌شود، نه با catchِ خطای UNIQUE — هم‌راستا با الگوی `products.ts`. ویرایش فقط حداقل/درصد را عوض می‌کند؛ عوض‌کردنِ سبد/کالا یعنی پله‌ی دیگری است، نه ویرایشِ همین.
  - `POST`/`PATCH`/`DELETE /api/volume-discounts` (staff، همان pageKeyِ `prices`).

### Added (v9 — مدیریتِ سبدِ قیمت‌گذاری از اکسل)
- **ورودِ اکسلِ قیمت** (`/staff/catalog` تبِ «ورودِ قیمت از اکسل»، pageKeyِ همان `prices` — نه گیتِ جدا). تا حالا تنها راهِ ثبتِ قیمت این بود که هر محصول را تک‌تک باز کنی و روی هر سبد قیمت بزنی؛ برای دهها قلم عملاً غیرقابلِ استفاده بود. حالا یک فایلِ اکسل (ستون‌های `sku`/`قیمت`) روی یک سبدِ انتخابی اعمال می‌شود — فایل در مرورگر پارس می‌شود (همان الگوی `ImportSection`)، فقط ردیف‌ها به سرور می‌روند. برخلافِ ورودِ موجودی **Snapshot نیست**: کالایی که در فایل نباشد دست‌نخورده می‌ماند، چیزی صفر نمی‌شود. ردپا (`audit_log`) فقط برای قیمت‌های واقعاً تغییرکرده نوشته می‌شود — دقیقاً قاعده‌ی همان POSTِ تک‌قلمیِ قدیمی.
- **ساختِ سبدِ قیمت‌گذاریِ تازه از UI** (`POST /api/price-lists`). تا حالا `price_list` فقط با SQL ساخته می‌شد — حتی خودِ پنلِ قیمت‌گذاری امکانِ ساختِ سبدِ تازه نداشت، فقط می‌توانست به سبدهای از‌قبل‌موجود قیمت بدهد. یک گپِ کوچکِ پیش‌نیاز بود: بدونِ آن، ورودِ اکسل هم به جایی برای اعمال‌شدن نداشت.
- `db/pricing.ts`: `createPriceList` + `applyPriceImport` (تراکنشِ واحد، خطای ردیف‌به‌ردیف مثلِ `imports.ts`: `unknown_sku`/`bad_price`، بقیه‌ی ردیف‌ها همچنان اعمال می‌شوند).

### Added (v9 — خروجیِ اکسل و PDF از گزارش‌ها/لجر/مشتریان)
- **حسابدارِ کارخانه با اکسل زندگی می‌کند** — تا حالا فقط ورودِ اکسل داشتیم (`/staff/import`)، خروج نداشتیم. سه بخش هرکدام دو دکمه گرفتند: «خروجیِ اکسل» و «خروجیِ PDF (چاپ)».
  - **اکسل**: کاملاً سمتِ کلاینت با همان `xlsx`ای که `/staff/import` برای پارسِ ورودی دارد — بدونِ کتابخانه‌ی تازه، بدونِ endpointِ صادرکننده‌ی جدا. `lib/exportXlsx.ts` یک تابعِ کوچکِ مشترک است (چند شیت در یک فایل). گزارش‌های مدیریتی سه شیت می‌دهد (عملکردِ نمایندگان/پرفروش‌ها/راکدها)، مشتریان دو شیت (پرخریدترین/همه‌ی مشتریان). لجر یک شیت می‌دهد ولی **همیشه کلِ نتیجه‌ی فیلترشده را می‌گیرد**، نه فقط صفحه‌ی روی صفحه — برای همین `GET /api/ledger` حالا `&limit` هم می‌پذیرد (سقفِ ۲۰٬۰۰۰، ضدِ درخواستِ سنگین).
  - **PDF**: هیچ کتابخانه‌ی PDF اضافه نشد — دکمه‌ی «خروجیِ PDF» همان `window.print()`ِ مرورگر را صدا می‌زند (مرورگر خودش «ذخیره به‌عنوانِ PDF» دارد)؛ فرم‌ها/دکمه‌ها/نوارِ تب با کلاسِ `no-print` (که برایِ برگه‌ی چاپیِ حواله‌ی v9 ساخته شده بود) از خروجیِ چاپ حذف می‌شوند.

### Added (v9 — صفحه‌بندی و جستجو در لیست‌های append-only پشتیبان)
- **حواله‌ها، backorderها، دفترِ حرکات، و دفترِ تغییرات دیگر پشتِ یک `LIMIT` ثابت گم نمی‌شوند.** این چهار لیست append-only-اند و فقط بزرگ‌تر می‌شوند؛ بدونِ صفحه‌بندی، بعدِ چند ماه کار، حواله/حرکتِ قدیمی از `LIMIT 50/100` ثابت بیرون می‌افتاد — نه خطا، نه نشانه، فقط دیگر «آنجا» نبود. هر چهار endpoint (`GET /api/sales-dispatches`, `/api/backorders`, `/api/ledger`, `/api/audit`) حالا `&offset` می‌گیرند و `hasMore` برمی‌گردانند (الگوی `limit+1`: یک ردیفِ اضافه می‌گیریم، اگر برگشت یعنی صفحه‌ی بعدی هست — بدونِ `COUNT(*)` جداگانه روی جدولی که می‌تواند بزرگ باشد). هر چهار جستجو (`&q`، ILIKE) هم دارند: حواله روی کد/مشتری/نمایندگی، backorder روی کالا/کد/نمایندگی/کدِ حواله، لجر روی کالا/کد/بچ، دفترِ تغییرات روی برچسبِ کالا/نمایندگی/شماره‌ی عامل. UI هر چهار جا یک باکسِ جستجو (debounce ۳۰۰ms) + دکمه‌ی «بیشتر» دارد.
- **عمداً دست‌نخورده ماندند:** صفِ «رزروهای در انتظار تأیید» و «درخواست‌های تأییدشده» — این‌ها صفِ کارِ زنده‌اند (فیلترِ `status`/`NOT EXISTS dispatch` نگه‌شان می‌دارد کوچک)، نه تاریخچه؛ مشکلِ «قدیمی از دست می‌رود» برایشان صدق نمی‌کند، پس اضافه‌کردنِ صفحه‌بندی به آن‌ها فقط پیچیدگیِ بی‌دلیل بود.

### Added (v9 — برگه‌ی چاپیِ حواله)
- **`/staff/dispatch/:id/print`.** انباردار کاغذ می‌خواهد، نه صفحه‌نمایش — تا حالا هیچ نمای چاپی‌ای برای لیستِ برداشت نبود. صفحه‌ی جدید: کدِ حواله/وضعیت/نمایندگی/مشتری/انبار/مقصد + جدولِ اقلام (کد، نام، درجه، **بچ**، شید، کالیبر، محلِ انبار، تعدادِ کارتن) + دو خطِ امضاء (انباردار / تحویل‌گیرنده-راننده). دکمه‌ی «چاپ» کنارِ هر حواله در `/staff` باز می‌شود (تبِ تازه، همیشه در دسترس — حتی حواله‌ی نهایی‌شده). `GET /api/sales-dispatches/:id` (staff، همان `authorizeStaff` پایه‌ی بقیه‌ی مسیرهای حواله) این جزئیات را می‌دهد. `@media print` بدونِ رنگ/سایه/دکمه، فقط سیاه روی سفید.

### Added (v9 — روزِ صفرِ مشتریِ تازه، بدونِ SQL)
- **مدیریتِ کاربران و نمایندگی‌ها در UI — ساختِ حساب/نماینده/انبار دیگر SQL دستی لازم ندارد.** انگیزه‌ی کاربر: «حساب را کارخانه می‌سازد ولی کارخانه ابزارش را ندارد» — هر مشتریِ تازه‌ی SaaS مهندس لازم داشت، که ضدِ خودِ ایده‌ی SaaS است. تیم/نمایندگی/انبار از قبل UI داشتند (v4)، ولی همه‌شان نیاز به یک admin از قبل‌موجود دارند؛ مشکلِ واقعی «روزِ صفر» بود: اولین tenant + اولین admin را چه کسی و از کجا بسازد.
  - نقشِ تازه‌ی **مدیرِ پلتفرم** بالاتر از سطحِ tenant: یک فلگِ ساده (`app_user.is_platform_admin`، نه یک سیستمِ احرازِ جداگانه) که با SQL/psql فقط **یک‌بار** در بدوِ راه‌اندازیِ خودِ SaaS ساخته می‌شود — نه به‌ازای هر مشتری.
  - صفحه‌ی `/platform/tenants`: عمداً فقط «ساختِ کارخانه‌ی تازه» — نه لیست/مدیریتِ tenantهای موجود (دامنه‌ی محدود، طبقِ تصمیمِ آگاهانه).
  - `db/platform.ts::createTenant()`: در یک تراکنش، tenant + اولین کاربرِ admin (با `findOrCreateUser` مشترک با مسیرِ دعوتِ تیم) + `tenant_membership` با `can_manage_access=true` می‌سازد — اولین مدیرِ هر کارخانه‌ی تازه از روزِ اول «مدیرِ دسترسی» هم هست، وگرنه همان بن‌بستِ v4 (هیچ‌کس نمی‌تواند دسترسیِ بقیه را تنظیم کند) در تنانتِ تازه تکرار می‌شد.
  - 🔴 **باگِ رول‌بکِ خودِ تستِ این فیچر پیدا کرد:** `sql.begin()` فقط با **throw** رول‌بک می‌کند، نه با `return`ِ معمولی از داخلِ callback — نسخه‌ی اولِ `createTenant` با `return {ok:false,...}` بعدِ ساختِ tenant، تراکنش را COMMIT می‌کرد و یک tenantِ یتیمِ بدونِ هیچ عضوی جا می‌گذاشت. رفع با throw کردنِ یک اکسپشنِ اختصاصی و catchِ بیرونِ تراکنش.
  - `login`: کاربری که هیچ عضویتی در هیچ tenantی ندارد ولی `isPlatformAdmin` است، به‌جای `/reserve` به `/platform/tenants` هدایت می‌شود.
  - `docs/GO_LIVE.md` بخشِ bootstrap به‌روزرسانی شد (دیگر SQL برای tenant+admin نیست، فقط برای خودِ حسابِ مدیرِ پلتفرم — یک‌بار).

### Fixed (بازبینیِ بقیه‌ی سایت — گزارش‌ها، مشتریان، کاتالوگِ نماینده، تیم)
- **LedgerSection: `reservation_cancel`** (نوشته‌شده در `db/reservations.ts` هنگامِ لغوِ رزرو) در نقشه‌ی فارسی‌سازی نبود — «حرکاتِ اخیر» به‌جای برچسب، متنِ خامِ انگلیسی نشان می‌داد.
- **`/staff/customers`:** بعدِ ثبتِ موفقِ مشتری، دراپ‌داونِ «نمایندگی» ریست نمی‌شد — مشتریِ بعدی (حتی مشتریِ مستقیمِ کارخانه) می‌توانست بی‌اراده به همان نمایندگیِ قبلی وصل شود.
- **`/catalogs` (نماینده):** دکمه‌ی «حذف» کاتالوگِ اشتراکی بدونِ تأییدِ دومرحله‌ای بود — برخلافِ الگوی یکدستِ `DeleteButton` که همه‌جای دیگرِ سایت استفاده می‌شود؛ یک کلیکِ اشتباه کاتالوگِ قیمت‌گذاری‌شده برای مشتری را بی‌بازگشت پاک می‌کرد.
- **🔴 `/staff/team`:** همان کلاسِ باگِ بالا، ولی جدی‌تر — بعدِ دعوتِ موفق، دراپ‌داونِ «نقش» به «پشتیبان» برنمی‌گشت. اگر مدیری کسی را «مدیر» دعوت می‌کرد و دراپ‌داون را دستی برنمی‌گرداند، عضوِ بعدی هم بی‌آنکه کسی متوجه شود با نقشِ مدیر دعوت می‌شد — واگذاریِ ناخواسته‌ی دسترسیِ کامل.

### Fixed (بازبینیِ /reserve و /reservations)
- **🔴 سبدِ رزرو می‌توانست ردیفِ نامرئی از موجودیِ فروش‌رفته نگه دارد.** اگر کاربر چیزی به سبد اضافه کند و بعد هر عملِ دیگری (خبرم‌کن، نوبت‌گرفتن) باعثِ رفرشِ `/api/lots` شود، و در همین فاصله موجودیِ آن لات توسطِ نماینده‌ی دیگری تمام شده باشد، آن لات از فهرست می‌افتد — ولی ردیفش در سبد می‌ماند: نه دیده می‌شود، نه قابلِ حذف، و همچنان در جمعِ سبد و در درخواستِ «ثبت رزرو (همه‌یا‌هیچ)» فرستاده می‌شود، که کلِ سبد را بی‌دلیل رد می‌کند. رفع: بعدِ هر بارگذاری، سبد با فهرستِ تازه هم‌گام می‌شود (حذفِ لاتِ ناموجود + کلمپِ مقدارِ اضافه) و اگر چیزی تغییر کرد، پیامِ صریح داده می‌شود. `QtyPicker` هم برای هم‌گام‌ماندنِ متنِ محلی‌اش با کلمپِ بیرونی اصلاح شد (بدونِ رقابت با تایپِ زنده‌ی کاربر، با یک ref که منشأِ تغییرِ `value` را تشخیص می‌دهد).

### Fixed (بازبینیِ داشبوردِ اصلیِ پنل پشتیبان)
- **صفِ «رزروهای در انتظار تأیید» با created_at DESC مرتب می‌شد** — یعنی رزروِ قدیمی‌تر که به انقضای TTL نزدیک‌تر است، زیرِ رزروهای تازه‌تر گم می‌شد و ممکن بود بدونِ تأیید منقضی شود. حالا با `expires_at ASC` (زودترین انقضا اول). API از قبل `expiresAt` را برمی‌گرداند ولی داشبورد اصلاً نشانش نمی‌داد؛ حالا همان بجِ مهلت (با رنگِ هشدار زیرِ یک ساعت، مثلِ صفحه‌ی «رزروهای من» نماینده) روی کارتِ صف هم هست. منطقِ محاسبه به `lib/date.ts` (`remainingTime`) منتقل شد تا دو صفحه یکی را کپی نکنند.
- **صفِ «درخواست‌های تأییدشده» (ساختِ حواله) هم همین مشکل را داشت** — از `created_at DESC` به `ASC` (FIFO) تغییر کرد تا درخواستِ نماینده‌ی اول زیرِ نماینده‌های تازه‌تر گم نماند.

### Added
- **ادغامِ «تیم/نمایندگی‌ها/انبارها/تأییدِ خودکار» زیرِ یک هابِ تب‌دار (`/staff/team`).** همان الگوی v6/v7 برای صفحاتِ راه‌اندازی/مدیریتی هم اعمال شد. تفاوتِ این خوشه با کاتالوگ/گزارش‌ها: گیتِ هر تب یکی نیست — تیم فقط برای «مدیرِ دسترسی» (`can_manage_access`)، نمایندگی‌ها/انبارها فقط برای admin، تأییدِ خودکار با همان pageKeyِ ریزدانه‌ی قبلی (`allowed_pages`). هر تب گیتِ خودش را جدا نگه داشت به‌جای یک قاعده‌ی مشترکِ نادرست. مسیرهای قدیمی (`/staff/agents`, `/staff/warehouses`, `/staff/auto-approve`) به تبِ معادل ریدایرکت می‌شوند.

### Fixed (پنلِ ادغام‌شده — QA زنده)
یک دورِ تستِ زنده به‌عنوانِ پشتیبان (ساختِ چند محصولِ آزمایشی با روش‌های مختلف) سه مشکلِ واقعی پیدا کرد که یونیت‌تست نمی‌گرفتشان چون همه به UI/تعاملِ کاربر مربوط بودند، نه منطقِ سرور:
- **🔴 پیامِ موفقیت قرمز و با آیکنِ خطا نشان داده می‌شد.** رنگِ بنر با حدسِ `msg.startsWith("محصول")` تعیین می‌شد؛ «محموله ثبت شد.» با «محموله» شروع می‌شود نه «محصول»، پس پیامِ موفقیت گمراه‌کننده به‌نظر می‌رسید. جایگزین شد با فلگِ صریحِ `msgOk` که هر فراخوانی آگاهانه ست می‌کند — نه حدسِ متن.
- **پیامِ نتیجه‌ی عملیات فقط بالای صفحه نشان داده می‌شد.** بعدِ ادغامِ پنج صفحه در یکی، کارتِ محصول می‌تواند خیلی پایین‌تر از آن بنر باشد؛ کاربر «تأیید رسیدن» می‌زد و هیچ بازخوردی نمی‌دید (چون باید اسکرول می‌کرد بالا). پیام حالا کنارِ خودِ کارتِ بازشده هم تکرار می‌شود.
- **پیامِ باقی‌مانده از یک کارت زیرِ پنلِ تازه‌بازشده‌ی کارتِ دیگر می‌ماند** — چون `msg` مشترک بود و شرطِ نمایش فقط «کدام کارت باز است» را می‌سنجید، نه «آیا این پیام واقعاً مالِ همین تعامل است». حالا با بازشدنِ هر پنلِ جدید پیامِ قبلی پاک می‌شود.
- **دکمه‌ی میان‌بُرِ «افزودنِ موجودی ←» روی نشانِ «بدون موجودی» گیتِ دسترسیِ pageKeyِ `incoming` نداشت** — برخلافِ دکمه‌ی اصلیِ «موجودی در راه» که درست گیت شده بود. پشتیبانی که فقط دسترسیِ «مدیریتِ محصول» دارد نه «موجودی در راه»، از همین میان‌بُر می‌توانست پنلِ ممنوعه را باز کند. گیت اضافه شد تا هر دو راهِ ورود به یک پنل، یک قانونِ دسترسی را رعایت کنند.

### Added
- **ثبتِ موجودیِ اولیه هنگامِ ساختِ محصول.** قبلاً تنها راهِ رساندنِ موجودیِ واقعی به یک محصولِ تازه، رفتن به صفحه‌ای دیگر (ورودِ اکسل یا موجودیِ در راه) بود — کاربر گزارش داد که بعدِ ساختِ محصول با «تعداد کارتن»/«هر متر کارتن» (که در واقع نسبتِ بسته‌بندی‌اند، نه موجودی)، محصول در صفحه‌ی نمایندگان «ناموجود» ماند. مشخص شد رفتارِ درست بود (`hasStock` از رویِ وجودِ `inventory_lot` می‌آید، نه از پرشدنِ آن دو فیلد) ولی جای واقعیِ ثبتِ موجودی کم بود. حالا فرمِ «محصول جدید» یک انبار+تعداد اختیاری دارد که همان مسیرِ لجرِ import/incoming را طی می‌کند (`inventory_lot`+`inventory_balance`+`inventory_transaction` با نوعِ `initial_stock`، در همان تراکنشِ ساختِ محصول) — نه یک UPDATE مستقیم بی‌ردِ حسابرسی.
- **ادغامِ پنج صفحه‌ی «کاتالوگ/قیمت‌گذاری/ورودِ اکسل/موجودیِ در راه/کالای جایگزین» به یک هابِ تب‌دار (`/staff/catalog`)، و سه صفحه‌ی «گزارش‌ها/دفترِ حرکات/دفترِ تغییرات» به هابِ دوم (`/staff/reports`).** انگیزه: پشتیبان برای مدیریتِ یک محصول مدام بینِ آدرس‌ها سوییچ می‌کرد. هر پنج/سه بخش حالا زیرِ یک مسیرند و با تب سوییچ می‌شوند، بدونِ افتِ دسترسیِ ریزدانه — چون `authorizeStaffPage` سرِ **هر فراخوانیِ API** چک می‌شود (نه فقط سرِ رندرِ route)، مخفی‌کردنِ تب همان تضمینِ قبلی را با گرانولاریتِ همان‌قدر ریز نگه می‌دارد. مسیرهای قدیمی (`/staff/prices`, `/staff/import`, `/staff/incoming`, `/staff/substitutes`, `/staff/ledger`, `/staff/audit`) به تبِ معادل ریدایرکت می‌شوند تا بوکمارک/لینکِ قدیمی نشکند. `NavMenu` هم به‌روزرسانی شد — لینک‌های تب‌دار به همان دو هاب با `?tab=` اشاره می‌کنند.

### Added
- **پشتیبانِ ثابتِ نمایندگی (Account Manager) برای ردیابیِ پورسانت.** هر نماینده حالا یک پشتیبانِ ثابتِ اختیاری دارد (`agent_account.assigned_staff_user_id`، admin از `/staff/agents` تعیین می‌کند). نام/شماره‌اش هم در پنلِ خودِ نماینده («پیگیرِ سفارش‌های شما» در `/reservations`) و هم در صفِ تأیید/درخواستِ پشتیبان نشان داده می‌شود — **حتی برای سفارش‌های تأییدِ خودکار**، چون پورسانت به همان پشتیبانِ ثابت تعلق می‌گیرد نه به کسی که دستی تأیید کرده. چت آنلاین عمداً به فازِ بعد موکول شد (نام/شماره برای شروع کافی است).
- **دسترسیِ ریزدانه‌ی سطحِ صفحه برای اعضای تیمِ پشتیبان (v4).** قبلاً هر عضوِ تیم به همه‌ی ده صفحه‌ی عملیاتی دسترسیِ کامل داشت. حالا مدیر می‌تواند برای هر پشتیبان مشخص کند دقیقاً به کدام صفحه‌ها دسترسی دارد (`tenant_membership.allowed_pages`؛ آرایه‌ی خالی = دسترسیِ کامل، سازگار با قبل). صفحه‌ای که دسترسی ندارد **اصلاً نشان داده نمی‌شود** (نه نمایش-بعد-منعِ‌کلیک) — بازخوردِ صریحِ کاربر بود که تجربه‌ی «لینک هست ولی کار نمی‌کند» گمراه‌کننده است. جدا از آن، یک فلگِ `can_manage_access` (فقط برای نقشِ admin، ملقب به «مدیرِ دسترسی» — بعدِ بازخوردِ کاربر از «معاونِ مدیر» تغییرِ نام یافت) مشخص می‌کند چه کسی اجازه دارد این دسترسی‌ها را برای بقیه تنظیم کند.
- **مدیریتِ کاملِ تیم/نمایندگی‌ها/انبارها با حذفِ واقعی.** سه صفحه‌ی جدید (`/staff/team`, `/staff/agents`, `/staff/warehouses`) برای admin: دعوت/ویرایش/حذفِ عضوِ تیم، ساخت/ویرایش/حذفِ نمایندگی، ساخت/ویرایشِ انبار. حذف همیشه با گاردِ «سابقه‌دار بودن» است (اگر رکورد در جدولی رفرنس شده، حذفِ واقعی رد می‌شود، نه silent no-op) به‌علاوه‌ی گاردهای اختصاصی: آخرین admin/مدیرِ دسترسی حذف نمی‌شود، عضوِ متصل به یک نمایندگی هم همین‌طور.

### Added
- **اعلان‌های چندکاناله (پیامک/ایمیل/بله).** `notification_outbox.channel` از فقط-پیامک به `sms`/`email`/`bale` گسترش یافت؛ هر سه فرستنده الگوی یکسانِ pluggable دارند (پیش‌فرض `log`، چون اعتبارنامه‌ی واقعی هنوز نداریم) — دقیقاً همان الگویی که برای پیامک ساخته شده بود، فقط تکرار برای دو کانالِ دیگر.

### Added
- **مدیریتِ کاملِ محصول — عکس همان‌جا که اطلاعات وارد می‌شود، نه بخشِ جدا.** بازخوردِ کاربر یک شکافِ عمیق‌تر را آشکار کرد: **محصول اصلاً از UI ساخته نمی‌شد** (نه نام، نه کد، نه هیچ‌چیز) — فقط seed یا SQL دستی، که برای production قابل قبول نیست. seed این را پنهان کرده بود.
  - صفحه‌ی «کاتالوگ تصویری» به **«مدیریت محصول»** تبدیل شد: فرمِ ساختِ محصول (نام، کد، sku، رنگ، لعاب، پانچ، بدنه) با **عکس در همان فرم** — thumbnail + آپلود کنارِ فیلدها، یا URL.
  - **قاعده‌ای که تست قفلش کرد:** ساختِ محصول هم‌زمان یک `product_variant` می‌سازد، وگرنه محصولِ بی‌واریانت هرگز در `/reserve` نمی‌آید — تله‌ای که فرمِ ساده پنهانش می‌کند.
  - **هر دو مسیرِ ورودی** (طبق خواسته‌ی کارخانه): فرمِ تک‌به‌تک، **و** ساختِ خودکار از import — اگر ستونِ «نام» در فایلِ اکسل باشد و sku ناموجود، محصول ساخته می‌شود. **بدونِ نام، همان خطای `unknown_sku` می‌ماند** — محصولِ بی‌نام نمی‌سازیم (احتمالاً sku اشتباه است). تست هر دو را قفل کرده.
  - ستونِ `product.body` (بدنه سفید/قرمز) به schema اضافه شد — مثل کاتالوگِ صنعتی.

### Fixed
- **`updateProduct` با `productId`ِ undefined، SQLِ خرابِ `UNDEFINED_VALUE` می‌ساخت** به‌جای خطای واضح. گارد اضافه شد که صریح fail کند. (در حین تست پیدا شد — تست `id:` می‌فرستاد به‌جای `productId:` و TypeScript از excess-property رد شد.)

### Added
- **کاتالوگ تصویری (v2) — الهام‌گرفته از بررسیِ سایتِ رقیب (طلاسرام).** بزرگ‌ترین کمبودِ ما نسبت به یک سایتِ ساده‌تر همین بود: نماینده روی صفحه‌ی ما کدِ `GB-6060` می‌دید، نه خودِ کاشی را. حالا هر محصول عکس دارد.
  - **زیرساخت از قبل بود:** `product.image_url` در schema وجود داشت و هیچ‌جا استفاده نمی‌شد. فقط سه لایه UI کم داشت.
  - **دو مسیرِ عکس، یک خروجی:** URL در ستونِ «عکس» فایلِ اکسلِ ورودِ موجودی، **یا** آپلودِ مستقیم در `/staff/catalog`. آپلود فایل را در `public/uploads/` ذخیره و **همان URL** را برمی‌گرداند، پس لایه‌ی نمایش نمی‌داند منبع کجاست و import اصلاً عوض نشد.
  - **کارت با thumbnail** (نه عکسِ تمام‌عرض): کارتِ سفارش است نه ویترینِ محض، و ورودیِ تعداد نباید زیرِ عکس گم شود. کلیک → **مودالِ جزئیات** با عکسِ بزرگ + رنگ/لعاب/پانچ/درجه (Esc یا کلیک روی پس‌زمینه می‌بندد).
  - قاعده‌ای که تست قفلش کرده: **ردیفِ بدونِ ستونِ عکس، عکسِ قبلی را پاک نمی‌کند** — وگرنه هر importِ معمولی همه‌ی عکس‌ها را خالی می‌کرد.
  - امنیتِ آپلود: نوعِ فایل از محتوا (نه پسوند)، سقفِ ۳MB، نامِ UUID (path-traversal بی‌اثر).

### Changed
- گاردِ `docsDrift` بلافاصله دو endpointِ جدید (`/api/products`, `/api/upload`) را گرفت و مجبور به مستندسازی‌شان کرد — دقیقاً کاری که برایش ساخته شد.

### Changed
- **نوار ناوبریِ پنل پشتیبان از ۱۰ لینکِ تخت به یک منوی گروه‌بندی‌شده تبدیل شد.** نکته این است که همبرگر فقط **ظرف** است؛ ارزشِ واقعی در **گروه‌بندی** است — منوی همبرگری با ده لینکِ تختِ داخلش فقط شلوغی را یک کلیک عقب می‌برد.
  - گروه‌بندی بر اساس **بسامدِ استفاده**، نه حروف الفبا: «کار روزمره» (ورود موجودی، موجودی در راه، مشتریان)، «تنظیمات فروش» (یک‌بار در راه‌اندازی: قیمت، سقفِ تأیید، جایگزین)، «گزارش و ردیابی» (وقتی سؤالی پیش بیاید)، «حساب کاربری».
  - **«خروج» عمداً بیرونِ منو ماند** — عملِ پرتکرار نباید پشتِ یک کلیکِ اضافه برود.
  - منو حالا در **همه‌ی زیرصفحه‌ها** هست: قبلاً رفتن از «قیمت‌گذاری» به «گزارش‌ها» دو کلیک بود (اول برگرد به پنل).
  - دسترسی‌پذیری: `aria-expanded`/`aria-controls`، بستن با Escape و کلیکِ بیرون، بازگشتِ فوکوس به دکمه، فوکوسِ خودکار روی اولین آیتم، و همه‌ی آیتم‌ها ≥۴۴px. صفحه‌ی فعلی با `aria-current` **به‌علاوه‌ی** نوارِ کناری و وزنِ فونت علامت می‌خورد — نه فقط رنگ.

### Fixed
- **قانونِ `.topbar nav a` به داخلِ پنلِ منو نشت می‌کرد.** آن قانون در پاسِ دسترسی‌پذیری `inline-flex` شده بود تا لینک‌های نوار به ۴۴px برسند، ولی چون به همه‌ی `a`های تودرتو هم می‌خورد، آیتم‌های منو به‌جای زیرِ هم **کنارِ هم** می‌نشستند. با محدودکردن به فرزندِ مستقیم (`.topbar nav > a`) ریشه‌اش بسته شد — نه با بالا بردنِ specificity.

### Fixed
- **🔴 شکستِ خاموشِ بارگذاریِ انبارها در «ورود موجودی» کاربر را به‌سمتِ گزینه‌ی مخرب هُل می‌داد.** `if (w.ok) setWarehouses(...)` بدونِ `else` بود، پس اگر فهرست نمی‌آمد دراپ‌داون **بی‌صدا خالی** می‌ماند. این فقط آزاردهنده نیست: کاربری که نمی‌تواند انبار انتخاب کند طبیعتاً دامنه را روی «کل کارخانه» می‌گذارد تا جلو برود — یعنی همان گزینه‌ای که موجودیِ **همه‌ی انبارها** را صفر می‌کند. حالا خطا صریح نشان داده می‌شود و **اعمالِ Snapshot قفل می‌شود** تا این مسیر بسته بماند. (تأیید با ۵۰۳ کردنِ موقتیِ API.)
- **صفحه‌ی «ورود موجودی» برای کاربرِ بدونِ نقشِ staff تا ابد روی «در حال بارگذاری…» می‌ماند** — همان هنگی که قبلاً در پنل پشتیبان بود. حالا پیامِ صریحِ دسترسی می‌دهد.

### Added
- **`Customer` به‌عنوان Entity کامل با تاریخچه (v2، spec ۹ — مشروط به ۷.۷).** با این، **v2 کامل شد**.
  - spec ارتقا از متنِ آزاد را مشروط کرده بود به «اگر گزارشِ پرخریدترین مشتری لازم شد» — پس همان گزارش دلیلِ وجودِ این جدول است و ماژول بیش از آن کاری نمی‌کند.
  - **`sales_dispatch.customer_name` حذف نشد، نقشش عوض شد:** حالا **snapshotِ** نامِ مشتری در لحظه‌ی حواله است. اصلاحِ نامِ مشتری نباید حواله‌ی صادرشده را بازنویسی کند — همان قاعده‌ی snapshotِ قیمت. تست قفلش کرده.
  - **حواله‌های قدیمیِ بدونِ Entity حذف نمی‌شوند:** زیر نامِ متنِ آزادشان و با نشانِ «وصل‌نشده» می‌آیند. کنار گذاشتنشان یعنی گزارش بی‌سروصدا کمتر از واقعیت نشان دهد.
  - **«ارزشِ نامعلوم» با «صفر» یکی گزارش نمی‌شود:** حواله‌ی backorder سفارش ندارد پس ارزشش معلوم نیست؛ جدا شمرده و صریح هشدار داده می‌شود (همان قاعده‌ی خط‌های بی‌قیمت در گزارش‌ها).
  - ارزش از snapshotِ سفارش و کارتن از لجرِ بارگیری — عمداً جدا، چون یکی تعهد است و دیگری تحویل.
  - مشتری **مالِ نمایندگی** است (`agent_account_id`)، تا نماینده‌ها فهرستِ مشتریانِ هم را نبینند. غیرفعال‌کردن جای حذف است، وگرنه حواله‌های گذشته مرجعشان را از دست می‌دهند. صفحه: `/staff/customers`.

### Added
- **موجودی در راه / پیش‌فروش تولید (v2، spec ۹) — «فلوی کامل‌تر از فلگ ساده».** آخرین موردِ بازِ v2.
  - **قیدِ سختی که رعایت شد:** این هرگز وارد `available` نمی‌شود. معادله‌ی `available = on_hand − held − allocated − blocked` دست‌نخورده ماند (spec ۱۹۸) — اگر موجودیِ نیامده available می‌شد، نماینده روی چیزی سفارش می‌داد که وجود ندارد و اولین کسی که واقعاً بار می‌خواست دستش خالی می‌ماند. تست قفلش کرده.
  - **آنچه در v1 کم بود «کِی» بود:** backorder فقط می‌گفت «در انتظار تولید». حالا نماینده کنارِ کالای ناموجود می‌بیند «۳۲۰ کارتن در راه — حدودِ ۲۰ مرداد (قطعی‌شده)»، درست کنارِ پیشنهادِ جایگزین — تا بتواند بین صبر کردن و گرفتنِ جایگزین انتخاب کند.
  - **رسیدن از مسیرِ لجر عبور می‌کند**، نه UPDATE مستقیم: همان الگوی import (`inventory_transaction` در همان تراکنشِ `inventory_balance`)، وگرنه گزارشِ تطبیق بلافاصله ناترازی نشان می‌داد. و چون موجودی آزاد شده، **صف انتظار همان لحظه جلو می‌رود** و «موجود شد» صف می‌شود — بدون این، محموله می‌رسید و کسی که ماه‌ها منتظرش بود خبردار نمی‌شد.
  - قفلِ ردیف در «رسید» تا دو کلیکِ هم‌زمان موجودی را دو برابر نکند؛ محموله‌ی دوم از همان بچ lotِ تکراری نمی‌سازد. مدیریت: `/staff/incoming`.

### Changed
- `prodlike-smoke.sh` بخش ۱۳ گرفت: ثبتِ محموله → رسید → افزایشِ `available` → ترازیِ لجر، همه زیر RLS.

### Added
- **پیشنهاد کالای جایگزین (v2، spec ۹).** وقتی کالایی ناموجود است، همان‌جا — نه در صفحه‌ای دیگر — جایگزین‌های **موجود** به نماینده نشان داده می‌شوند.
  - **چرا جدولِ صریح و نه تطبیقِ خودکار بر اساس صفت:** هیچ فیلدِ ساختاریافته‌ای برای **ابعاد** نداریم (اندازه فقط داخلِ نام/کد است)، و `sqcm_per_box` مساحتِ کارتن است نه ابعاد — یک ۶۰×۶۰ و یک ۳۰×۱۲۰ می‌توانند مساحتِ یکسان داشته باشند و هرگز جایگزینِ هم نیستند. `color`/`glaze`/`grade` هم nullable‌اند و import پرشان نمی‌کند (در داده‌ی واقعی همه خالی بودند). حدسِ ماشینی اینجا یعنی پیشنهادِ اشتباه — بدتر از پیشنهاد ندادن.
  - **تنها تطبیقِ خودکارِ قابل‌اعتماد** «همان محصول با درجه‌ی دیگر» است (`product_id` یکی، `grade` فرق دارد) — از نظر ساختاری تضمین‌شده همان کاشی است و صفر تنظیم می‌خواهد.
  - **هرگز جایگزینِ ناموجود پیشنهاد نمی‌شود** — نماینده را دو بار ناامید می‌کند. تست قفلش کرده.
  - رابطه **جهت‌دار** است: «اگر گران نبود ارزان را بده» لزوماً برعکسش درست نیست.
  - تعریفِ صریحِ کارخانه جلوتر از حدسِ خودکار می‌آید و اگر همان مورد را پوشش دهد، تکراری نمی‌شود. مدیریت: `/staff/substitutes`.

### Added
- **«خروج از همه‌ی دستگاه‌ها»** در `/account/password` (که حالا «امنیت حساب» است). زیرساختش از تغییرِ رمز آماده بود. رمز نمی‌خواهد و لازم هم ندارد: اگر مهاجمی با نشستِ دزدیده این را بزند، **نشستِ خودش هم می‌میرد** و فقط کسی که رمز را دارد می‌تواند دوباره وارد شود — یعنی برای مهاجم خودزنی است. دستگاهِ فعلی داخل می‌ماند (کوکیِ تازه صادر می‌شود)، چون کسی که نگرانِ نشستِ دزدیده است نباید برای بیرون‌کردنِ او خودش هم بیفتد بیرون.

### Fixed
- **🔴 حفره‌ی مرزِ ثانیه در باطل‌سازیِ نشست.** معیار یک `sessions_valid_from` ثانیه‌گرد بود با شرطِ `iat < valid_from`. چون `iat` در JWT **ثانیه‌ای** است، توکنی که در همان ثانیه‌ی باطل‌سازی صادر شده بود از فیلتر رد می‌شد و **برای همیشه** زنده می‌ماند — پنجره‌ای باریک با اثرِ دائمی، دقیقاً در فیچری که برای بستنش ساخته شده بود. در تستِ دستی دیده شد: توکنِ قدیمی بعد از «خروج از همه» هنوز ۲۰۰ می‌گرفت، و با ۳ ثانیه فاصله ۴۰۱. جایگزین شد با **شمارنده‌ی نسخه** (`app_user.session_epoch`) و مقایسه‌ی **تساوی**: عدد granularity ندارد، پس مرز کاملاً حذف می‌شود. افزایش هم اتمیک در SQL (`+ 1`) است تا دو باطل‌سازیِ هم‌زمان یکدیگر را بازنویسی نکنند.

### Added
- **دفتر تغییراتِ قواعدِ پولی (`audit_log`).** جدولش از ابتدا وجود داشت ولی **هیچ کدی در آن نمی‌نوشت**. دامنه عمداً باریک است: بیشترِ کارها از قبل ردپا دارند (حرکتِ موجودی → `inventory_transaction`، دلیلِ تأیید → `approval_mode`، ورودِ اکسل → `import_batch`). چیزی که هیچ‌جا ثبت نمی‌شد، تغییرِ خودِ قواعد بود — **قیمت** (کسی قیمت را ده‌برابر پایین می‌آورد و هیچ‌کس نمی‌فهمید چه کسی) و **سقفِ تأیید خودکار** (کنترلی که اجازه می‌دهد پول بدونِ نگاهِ انسان متعهد شود؛ بالابردنش بدونِ ردپا یعنی می‌شد سقف را برداشت، سفارشِ بزرگ خودکار تأیید شود، و سقف را برگرداند). ردپا در **همان تراکنشِ** تغییر نوشته می‌شود و ذخیره‌ی بی‌تغییر ردپا نمی‌سازد. صفحه: `/staff/audit`.

### Fixed
- **🔴 `import_row.raw_data` دوبار encode می‌شد.** الگوی `${JSON.stringify(x)}::jsonb` باعث می‌شد postgres.js مقدارِ از قبل stringify‌شده را دوباره JSON کند، پس در دیتابیس یک jsonb از نوعِ **string** می‌نشست نه object. یعنی استخراجِ فیلد از آن همیشه NULL می‌داد — و آن ستون دقیقاً برای سؤالِ «در فایل چه بود؟» وجود دارد. با `tx.json(...)` اصلاح شد. (همین اشکال در دفترِ تغییرات هم بود و عدد را به رشته تبدیل می‌کرد؛ تست حالا قفلش می‌کند.)

### Fixed
- **🔴 کدِ بازیابی رمز هرگز فرستاده نمی‌شد (فقط در production).** `requestReset` مستقیم روی `notification_outbox` و `tenant_membership` می‌نوشت، ولی هر دو `tenant_id` دارند یعنی RLS دارند و `app.tenant_id` ست نشده بود. زیر نقشِ واقعیِ اپ نتیجه `INSERT 0 0` بود — **خطا نمی‌داد، فقط سکوت**: نماینده «کد فرستاده شد» می‌دید و تا ابد منتظرِ پیامکی می‌ماند که نمی‌آمد. dev با superuser است و RLS را دور می‌زند، پس فقط در production ظاهر می‌شد — همان تله‌ای که قبلاً workerِ پیامک را زمین زده بود. اصلاح: tenant با `user_contexts` (SECURITY DEFINERِ موجود) پیدا می‌شود و نوشتن داخل `withTenant` انجام می‌گیرد. همه‌ی مسیرهای دیگر بررسی شدند؛ این تنها مورد بود.
- **🔴 خودِ `prodlike-smoke.sh` دروغ می‌گفت.** `cleanup()` فقط پوسته‌ی `npx` را می‌کشت نه فرزندِ `next start` را، پس هر اجرای ناموفق یک سرورِ zombie جا می‌گذاشت و اجرای بعدی — بدونِ اینکه بتواند پورت را بگیرد — به همان سرورِ **قدیمی** curl می‌زد و سبز می‌شد. یعنی تست به‌جای کدِ جدید، باینریِ قبلی را تأیید می‌کرد. حالا پورت قبل از شروع پاک می‌شود و اگر باز اشغال بود اسکریپت **شکست می‌خورد** به‌جای ادامه دادن.

### Changed
- **`prodlike-smoke.sh` پوششِ v2 را گرفت** (بخش‌های ۸–۱۲): بازیابی رمز و رسیدنِ کد به Outbox، باطل‌شدنِ نشستِ قدیمی، صف انتظار (لغو → پیشنهادِ خودکار → پیام)، تأیید هیبریدی و ثبتِ `approval_mode=auto`، و گزارش‌ها/تنظیمات. همه‌ی این‌ها از اجرای قبلیِ اسکریپت اضافه شده بودند و **هرگز زیر RLS اجرا نشده بودند**.

### Added
- **تغییر و بازیابی رمز عبور.** مسئله‌ی اصلی UI نبود، امنیت بود: JWT هیچ راهی برای **باطل‌شدن** نداشت، پس تغییرِ رمز نشستِ دزدیده‌شده روی دستگاهِ دیگر را نمی‌کشت و تا ۷ روز زنده می‌ماند — یعنی امنیتِ نمایشی. حالا `app_user.sessions_valid_from` اضافه شده و `currentUserId` هر توکنی را که `iat`اش قدیمی‌تر باشد رد می‌کند (و کاربرِ غیرفعال‌شده را هم). هر دو مسیرِ تغییرِ رمز در **همان تراکنش** نشست‌ها را باطل می‌کنند.
  - **تغییر رمز** (`/account/password`) با رمز فعلی؛ کوکیِ خودِ کاربر دوباره صادر می‌شود تا از سایتِ خودش پرت نشود، بقیه‌ی دستگاه‌ها بیرون می‌مانند.
  - **بازیابی** (`/reset`) با کدِ شش‌رقمیِ پیامکی روی همان زیرساختِ Outbox. کد **hash** ذخیره می‌شود نه خودش، یک‌بارمصرف است، ۱۰ دقیقه اعتبار دارد، و بعد از ۵ حدس می‌سوزد (کدِ شش‌رقمی وگرنه قابلِ جست‌وجوی کامل است). درخواستِ تازه، کدِ قبلی را باطل می‌کند و تغییرِ رمز هم کدهای در جریان را می‌کشد.
  - **هیچ‌کدام وجودِ شماره را لو نمی‌دهند** — پاسخ چه کاربر باشد چه نباشد یکی است، وگرنه این endpoint ابزارِ شمارشِ شماره‌های نمایندگان می‌شد.
  - ۱۵ تست، متمرکز بر راه‌های شکست. تأیید در مرورگر: توکنِ «دستگاه دوم» بعد از تغییرِ رمز ۴۰۱ گرفت، دستگاهِ فعلی داخل ماند، و کدِ مصرف‌شده دوباره رد شد.

### Fixed
- **پیامکِ `waitlist_offer` قالب نداشت** و به‌جای متنِ درست «اعلان جدید» فرستاده می‌شد.
- **worker‌ها `.env` را نمی‌خواندند** (`worker:expire` / `worker:outbox`) و لوکال به دیتابیس وصل نمی‌شدند. با `--env-file-if-exists` حل شد — `if-exists` چون در production متغیرها از systemd می‌آیند و فایلی وجود ندارد.

### Changed
- **پاسِ دومِ UI/UX — صفحه‌های باقی‌مانده و چیدمانِ پنل:**
  - **ورود:** فرمِ دوفیلدی در عرضِ ۹۰۰px چسبیده به بالا بود؛ حالا کادرِ ۳۸۰px وسطِ صفحه.
  - **پنل پشتیبان دوستونه شد** (بالای ۹۰۰px): سمتِ راست کارهایی که منتظرِ **تصمیمِ** پشتیبان‌اند (صفِ تأیید، حواله‌سازی)، سمتِ چپ چیزهایی که فقط **پیگیری** می‌شوند (حواله‌ها، backorder). قبلاً «حواله‌ها» همیشه زیرِ خطِ تا بود. زیرِ ۹۰۰px یک‌ستونه می‌شود و ستونِ کارها اول می‌آید.
  - **ورود موجودی** به سه گامِ شماره‌دار تقسیم شد (فایل → دامنه → اعمال) و ماهیتِ مخربش قبل از هر فیلدی گفته می‌شود.
  - **قیمت‌گذاری:** «‏X از Y کالا قیمت دارد» روی هر لیست، و دکمه‌ی ذخیره تا وقتی چیزی عوض نشده غیرفعال است.

### Fixed
- **🔴 خطای ورود، کدِ خامِ سرور را نشان می‌داد:** برای ۴۰۰ رشته‌ی `"invalid"` عیناً به کاربر نمایش داده می‌شد. حالا پیام از روی **status** ساخته می‌شود (۴۰۱/۴۰۰/۴۲۹ هرکدام متنِ فارسیِ خودش).
- **پشتیبان بعد از ورود به صفحه‌ی نماینده می‌رفت** و بنرِ «به نمایندگی‌ای وصل نیستی» می‌گرفت — درست ولی گیج‌کننده. حالا مقصد بر اساس نقش تعیین می‌شود.
- **ردیف‌های ناخوانده‌ی فایلِ اکسل بی‌سروصدا کنار گذاشته می‌شدند.** در یک snapshot این خطرناک است: ردیفی که خوانده نشود «غایب» حساب می‌شود و موجودی‌اش **صفر می‌رود**. حالا شمرده و با هشدار نشان داده می‌شود.
- **دامنه‌ی «کل کارخانه» هیچ هشداری نداشت** با اینکه می‌تواند موجودیِ همه‌ی انبارها را صفر کند (spec ۱۴.۵). حالا بنرِ قرمز + دکمه‌ی `danger`.
- **`zeroed` در نتیجه‌ی import** با بقیه‌ی اعداد هم‌وزن بود؛ حالا وقتی بزرگ‌تر از صفر است رنگِ هشدار می‌گیرد.

### Changed
- **پاسِ UI/UX — سیستمِ طراحی به‌جای استایلِ پراکنده:** جهت «ابزارِ کاریِ صنعتی» انتخاب شد، نه صفحه‌ی فرود. پالت از ui-ux-pro-max («Industrial slate + stock green») ولی **سبکِ پیشنهادی‌اش عمداً رد شد**: «Exaggerated Minimalism» با وزن ۹۰۰ و تیترِ `clamp(3rem,10vw,12rem)` برای لندینگِ برند است و اینجا فقط جدول را از صفحه بیرون می‌انداخت. توکن‌های معنایی (رنگ/فاصله/شعاع/سایه)، حالتِ تیره، و کلاس‌های مشترک `card / banner / badge / empty / topbar`.
- **فونت فارسی، self-hosted:** `globals.css` از قبل `Vazirmatn` را نام می‌برد ولی هیچ‌وقت بارگذاری نمی‌شد — یعنی همه‌چیز روی Tahoma می‌افتاد. حالا `@fontsource-variable/vazirmatn` از node_modules می‌آید: نه `next/font/google` و نه `@import` از CDN، چون هر دو در build به گوگل fetch می‌زنند و روی VPS ایران شکننده‌اند. `line-height` به ۱.۷ رفت (فارسی در ۱.۵ به هم می‌چسبد).

### Fixed
- **دکمه‌ی غیرفعال، غیرفعال به‌نظر نمی‌رسید** و `cursor: progress` می‌گفت «در حال کار» در حالی که واقعیت «هنوز عدد وارد نکرده‌ای» بود — یک استایل برای دو معنیِ متفاوت. حالا `:disabled` (کم‌رنگ + `not-allowed`) از `[aria-busy]` (کدر + `progress`) جداست.
- **کنتراستِ زیرِ استاندارد:** متنِ کم‌رنگ ۲.۵۶:۱ و badge ۴.۳۴:۱ بودند — هر دو زیر ۴.۵:۱، در حالی که کدِ کالا و وضعیت اطلاعاتِ واقعی‌اند نه تزئین. پله‌ی خاکستری تیره‌تر شد؛ حالا در روشن و تیره همه بالای ۴.۵ (اندازه‌گیری‌شده در مرورگر).
- **لینکِ ناوبری ۳۴px** بود (زیر ۴۴px)، روی موبایل هدفِ لمسیِ کوچک.
- **ارقامِ مخلوط:** «۱۳۵ کارتن (1 پالت + 15 کارتن)» دو الفبای عددی در یک خط داشت.
- **ایموجی به‌جای آیکن** (⚠️ 🔔 ⏳ ✓) با SVGی درون‌خطی جایگزین شد: ایموجی روی ویندوزهای رایج در ایران شکلِ دیگری دارد، رنگش را از توکن نمی‌گیرد و صفحه‌خوان اسمِ کاملش را می‌خواند.

### Fixed
- **🔴 یک حواله می‌توانست اقلامی از دو انبار داشته باشد.** مدلِ داده از اول چندانباره بود (`inventory_lot.warehouse_id`)، ولی `createDispatchFromRequest` انبار را نادیده می‌گرفت — یعنی انباردار لیستِ برداشتی می‌گرفت که نصفش آنجا نبود، و حواله هرگز کامل نمی‌شد. حواله یعنی یک کامیون که در **یک** نقطه بار می‌زند.

### Added
- **چندانباره (v2، spec ۹):** سفارشی که از دو انبار تأمین شود خودکار به **دو حواله** تقسیم می‌شود، هرکدام با کدی که پسوندِ کدِ انبار دارد (`D-1404-002-W1`, `-W2`) تا برای انباردار معنادار بماند؛ سفارشِ تک‌انباره کدش دست‌نخورده می‌ماند. `sales_dispatch.warehouse_id` اضافه شد (NULL برای backorder — کالایی که هنوز تولید نشده انبار ندارد). سمتِ نماینده: انبار روی هر قلم نشان داده می‌شود، فیلترِ انبار (فقط وقتی بیش از یکی هست)، و هشدارِ نرم وقتی سبد چندانباره است — ممنوع نیست، ولی نماینده باید از قبل بداند دو حواله می‌گیرد. پنل پشتیبان انبارِ هر حواله را نشان می‌دهد و بعد از تقسیم صریح می‌گوید چند حواله ساخته شد. ۴ تست، از جمله ثابت‌کردنِ اینکه هیچ حواله‌ای دو انباره نیست و بارگیریِ یکی موجودیِ آن یکی را دست نمی‌زند.
- **صف انتظار (v2، spec ۹: «صف انتظار برای رزروهای آزادشده»):** تفاوتش با `stock_alert` این است که آن «خبرم کن» است و به همه پخش می‌شود — هرکس زودتر کلیک کرد می‌برد، یعنی نماینده‌ای که سه روز منتظر بوده به کسی می‌بازد که اتفاقی صفحه‌اش باز بوده. صف **منصف** است: به‌ترتیبِ نوبت، به‌محض آزاد شدن موجودی همان تعداد برای نفرِ اول **رزرو می‌شود** و پیامش در همان تراکنش صف می‌شود. نکته‌ی درستی: `held` فقط رزروِ `active` را می‌شمارد، پس موجودی در همان لحظه‌ی لغو آزاد می‌شود — بنابراین `advanceWaitlist` باید در همان تراکنشِ آزادسازی اجرا شود، وگرنه پنجره‌ای می‌ماند که صف را بی‌معنا می‌کند. برای همین `reserveIn` از `reserve` جدا شد. سه نقطه‌ی آزادسازی وصل شده: لغو، انقضا (worker)، و ورودِ موجودی — و در import **قبل از** پخشِ «موجود شد»، چون صف حقِ تقدم دارد. پیشنهادِ صف عمداً `skipAutoApprove` است: نماینده در آن لحظه حاضر نیست و تعهدِ پول نباید بدونِ او انجام شود. ۹ تست، متمرکز بر انصاف: ترتیبِ صف، all-or-nothing (نوبت با نصفِ سفارش سوزانده نمی‌شود)، و اینکه کلیکِ دوباره نوبت را جلو نمی‌اندازد.

### Changed
- `expire_due_reservations()` به‌جای یک شمارش، `(tenant_id, variant_id)`های آزادشده را برمی‌گرداند — worker بدونِ آن می‌دانست «چیزی آزاد شد» ولی نه «چه چیزی»، و صف هرگز حرکت نمی‌کرد.
- NOTICEهای پرحرفِ postgres خاموش شدند (`DEBUG_PG_NOTICE=1` برشان می‌گرداند).
- **تأیید هیبریدی (v2، spec ۹):** سفارشی که ارزشش زیرِ سقف باشد **در همان تراکنشِ رزرو** خودکار تأیید می‌شود؛ بالای سقف مثل قبل به صفِ پشتیبان می‌رود. سقف: `agent_account.auto_approve_limit` (NULL = ارث، `۰` = هرگز) بر `tenant.auto_approve_limit` (NULL = خاموش) ارجح است — `۰` جای یک ستونِ بولیِ جداگانه را می‌گیرد چون هر سفارشی بزرگ‌تر از صفر است. **پیش‌فرض خاموش**: فیچری که پول را بدونِ نگاهِ انسان متعهد می‌کند نباید با نصبِ ساده روشن شود. **همه‌ی مسیرهای ابهام به تأییدِ دستی می‌روند**: سقفِ تعریف‌نشده، خطِ بی‌قیمت، یا کالای بدونِ لیستِ قیمت. `sales_request.approval_mode` + `auto_approve_limit_applied` ثبت می‌کنند چرا و با چه سقفی تأیید شده (اولین سؤالِ کارخانه وقتی سفارشی بدونِ او تأیید شده همین است)، و پنل پشتیبان نشانِ «تأیید خودکار» می‌زند. تنظیم از `/staff/auto-approve`. ۹ تست، متمرکز بر مرزها: دقیقاً روی سقف، یک ریال بالاتر، `۰` در برابر NULL، خطِ بی‌قیمت، و اینکه تخفیف حجمی در سنجشِ سقف همان عددی باشد که snapshot می‌شود.
- **تقویم شمسی در کلِ سایت:** هر تاریخی که به کاربر نشان داده می‌شود جلالی است و **همیشه با `Asia/Tehran`** رندر می‌شود، نه timezone مرورگر (spec ۱۴.۸) — بدون این، کاربری که ساعتِ سیستمش روی منطقه‌ی دیگری است تاریخِ اشتباه می‌بیند. `lib/date.ts` تقویم را خودش پیاده نمی‌کند؛ `Intl` مرجعِ حقیقت است و تبدیلِ معکوس (شمسی→میلادی) با حلقه‌ی تصحیح به خودِ `Intl` گره خورده، نه به ریاضیِ دستیِ چرخه‌ی کبیسه‌ی ۳۳ساله. `JalaliDateInput` سه `<select>` ساده است (`<input type="date">` فقط میلادی است و چاره‌ای نبود) — روزهای ماه از خودِ تقویم می‌آید، پس ۳۱ اسفندِ سالِ غیرکبیسه اصلاً قابلِ انتخاب نیست. ۵ تست، شامل رفت‌وبرگشتِ ~۲۲۰۰ روزِ پیوسته.
- **`db/seed-dev.sql`** — بازسازیِ داده‌ی نمایشیِ محیطِ توسعه با یک فرمان. قبلاً دستی ساخته شده بود و با یک اجرای تست از بین رفت.

### Added (پیش‌تر)
- **لغو رزرو (نماینده/پشتیبان):** state machineی spec بخش ۶ گذارِ `active → cancelled` را داشت و schema هم اجازه‌اش را می‌داد، ولی **هیچ کدی آن را ست نمی‌کرد** — یعنی نماینده‌ای که اشتباهی ۳۰۰ کارتن رزرو می‌کرد، تا پایان TTL (۲۴ ساعت) موجودی را قفل می‌کرد. `cancelReservation()` + `POST /api/reservations/:id/cancel` (با `agentAccountId` → نماینده فقط رزروِ خودش؛ بدون آن → پشتیبان هر رزرو) + دکمه در `/reservations` و `/staff`. موجودی **بلافاصله** آزاد می‌شود چون `held` محاسباتی است — نیازی به دست‌زدن به balance نیست. ۳ تست (۳۷/۳۷).
- **`docs/GO_LIVE.md`** — چک‌لیستِ کاملِ آنلاین‌شدن: نقش‌های DB (اپ نباید superuser یا مالکِ جدول باشد وگرنه RLS دور می‌خورد)، env، HTTPS اجباری (کوکی در production `Secure` است)، پروکسی و `X-Forwarded-For` برای rate limit، سقفِ limiter در حالت چند-instance، دو cron، بک‌آپ ایران→خارج با retry، سیاست migration، SQL راه‌اندازیِ اولین کارخانه/کاربر (فرم ثبت‌نام عمداً نداریم)، تصمیم‌های بازِ مصاحبه، و تست دودِ بعد از دیپلوی.
- **دفتر حرکات موجودی + تطبیق (`/staff/ledger`, `/api/ledger`):** لجری که از روز اول در هر عملیات می‌نوشتیم حالا خوانا شد — دلیلِ وجودش طبق spec ۵.۴ («چرا موجودی این کالا اشتباهه؟»). گزارشِ **ناترازی**: برای هر Lot باید `SUM(on_hand_delta) == on_hand` و `SUM(allocated_delta) == allocated`؛ هر اختلافی یعنی موجودی از مسیری عوض شده که لجر ننوشته. تست: کلِ زنجیره (import→رزرو→تأیید→بارگیری) ناترازیِ صفر می‌دهد، و UPDATE دستی دقیقاً گرفته می‌شود.

### Fixed
- **🔴 `npm test` می‌توانست پایگاه‌داده‌ی توسعه را پاک کند:** `resetSchema()` روی `DATABASE_URL` فرمانِ `DROP SCHEMA public CASCADE` می‌زند. تست‌ها env را از فایل نمی‌خواندند، پس تا امروز **تصادفاً** به سرورِ ناموجودِ ۵۴۳۲ وصل می‌شدند و DBِ توسعه سالم می‌ماند؛ هرکس `DATABASE_URL` را در شلش export می‌کرد، داده‌اش را از دست می‌داد (همین اتفاق افتاد). حالا تست‌ها با `--env-file=.env.test` روی DBِ جداگانه‌ی `tile_test` اجرا می‌شوند و `assertTestDatabase()` اگر نامِ DB به `_test` ختم نشود اصلاً اجازه‌ی DROP نمی‌دهد.
- **مرزِ روز در فیلترِ گزارش، نیمه‌شبِ UTC بود نه تهران:** «از ۱ مرداد» بی‌سروصدا سه‌ونیم ساعتِ اولِ آن روز را از گزارش می‌انداخت. `jalaliToDate` حالا نیمه‌شبِ تهران برمی‌گرداند (تست دارد).
- **الگوی fail-open در همه‌ی صفحه‌ها بسته شد (کلاسِ باگ، نه یک مورد):** `if (res.ok) setX(...)` روی خطا حالتِ خالی نشان می‌داد، یعنی صفحه بی‌سروصدا دروغ می‌گفت. خطرناک‌ترینش پنل staff بود: روی خطای بارگذاری می‌گفت «رزروِ فعالی برای تأیید نیست» در حالی که نماینده منتظر تأیید بود — ضررِ کسب‌وکاریِ واقعی، نه فقط زشتیِ UI. حالا helper مشترک `lib/api.ts` («خالی» را از «نشد» جدا می‌کند)، بنرِ خطای صریح، و پیام‌های «چیزی نیست» فقط بعد از بارگذاریِ موفق نشان داده می‌شوند. اعمال‌شده روی `/staff`، `/reserve`، `/reservations`، `/staff/ledger`.
- **صفحه‌ی تطبیق fail-open بود:** روی خطای بارگذاری (مثلاً ۴۰۳ برای نماینده) سبز نشان می‌داد «✓ تراز است» — یعنی صفحه‌ای که ادعای ایمنی می‌کند، وقتی هیچ داده‌ای نگرفته بود هم مطمئن به نظر می‌رسید. حالا «بارگذاری موفق و خالی» از «بارگذاری ناموفق» تفکیک شده.

### Added (قبلی)
- **StockAlert + Outbox پیامک (آخرین فیچرِ v1، spec ۵.۹):** نماینده روی کالای ناموجود «خبرم کن» می‌زند؛ به‌محض اینکه import موجودی بیاورد، پیام **در همان تراکنشِ import** صف می‌شود (الگوی Outbox — اگر import رول‌بک شود پیامِ دروغینِ «موجود شد» هم نمی‌ماند) و `stock_alert` مصرف می‌شود (یک‌بارمصرف، پس اسپم ممکن نیست). worker `npm run worker:outbox` با الگوی claim-then-send: claim اتمیک با `FOR UPDATE SKIP LOCKED` (دو worker یک پیام را دوبار نمی‌فرستند)، ارسال بیرون از تراکنش، retry تا ۵ بار و بعد dead-letter (`failed`). فرستنده pluggable است (`SMS_PROVIDER`، پیش‌فرض `log`) چون اعتبارنامه‌ی پنل ایرانی نداریم — فقط همان یک تابع برای پروداکشن عوض می‌شود. ۴ تست جدید (۳۲/۳۲).

### Added (قبلی)
- **چرخه‌ی انقضای رزرو + خروج:** worker بوک‌کیپینگِ انقضا (`expire_due_reservations()` + `npm run worker:expire` برای cron هر ۱۰-۱۵ دقیقه، spec ۵.۳) که رزروهای از مهلت گذشته را `active→expired` می‌کند — **درستیِ `available` به آن وابسته نیست** (held همیشه `expires_at > now()` را شرط می‌کند)، پس تأخیر/شکستش بی‌خطر است. خروج (`POST /api/auth/logout`، POST نه GET تا با CSRF نشود کاربر را خارج کرد) + دکمه‌ی خروج در صفحات نماینده و staff.

### Fixed
- **صفِ تأیید، رزروهای منقضی را «در انتظار تأیید» نشان می‌داد** — کوئری فقط `status='active'` را فیلتر می‌کرد و چون worker انقضا وجود نداشت، رزروِ منقضی برای همیشه `active` می‌ماند. حالا (۱) صفِ staff شرطِ `expires_at > now()` دارد و (۲) وضعیتِ نمایشی در SQL مشتق می‌شود (`CASE ... THEN 'expired'`) تا حتی با تأخیرِ worker هم درست باشد. این دقیقاً همان چیزی است که spec هشدار داده بود: هرگز فقط به `status` تکیه نکن.

### Added (قبلی)
- **سخت‌سازیِ امنیت (spec ۸):** rate limiting روی لاگین (دولایه: per-IP ۲۰/۱۵دقیقه + per-phone ۵/۱۵دقیقه — جلوی brute-force از IPهای چرخان)، رزرو (۳۰/دقیقه per-user) و import (۱۰/ساعت). پاسخ ۴۲۹ با `Retry-After`. هدرهای امنیتی در `next.config.ts`: `X-Frame-Options: DENY` + CSP `frame-ancestors 'none'`، `nosniff`، `Referrer-Policy`، `Permissions-Policy`، و حذف `X-Powered-By`. ۳ تست واحد + تأیید زنده روی سرور (۵ تلاش → ۴۲۹ با Retry-After 899).

### Fixed
- **پنل staff روی «در حال بارگذاری…» قفل می‌شد** — `user_contexts()` با INNER JOIN به `agent_account_user` بسته شده بود، پس کاربرِ staff (که به هیچ نمایندگی وصل نیست) صفر context می‌گرفت و همه‌ی صفحه‌ها منتظر می‌ماندند. با LEFT JOIN + برگرداندن `role` حل شد؛ صفحات نماینده هم حالا نبودِ `agentAccountId` را با پیام مدیریت می‌کنند به‌جای هنگ. این باگ فقط با اجرای واقعیِ اپ در مرورگر پیدا شد (تست‌های API چون `tenantId` را مستقیم می‌فرستادند از کنارش رد می‌شدند). تست رگرسیون schema (تست ۶) اضافه شد.

### Added (قبلی)
- **Import اکسل (snapshot، spec ۱۴.۵):** موتور تطبیقِ اتمیک `applySnapshot` — درد اصلیِ کارخونه (اکسل دستی). قواعد خطرناک رعایت‌شده و تست‌شده: فقط `on_hand`؛ ردیفِ غایب فقط **داخل scope اعلام‌شده** صفر می‌شه (نه کل tenant)؛ `on_hand` هرگز زیر `allocated+blocked` نمی‌ره (guardِ below_committed + absent_but_committed)؛ idempotent با `ImportBatch`. پارسِ `.xlsx` سمتِ **مرورگر** (SheetJS) → هیچ فایلی به سرور نمی‌رسه (سطحِ حمله‌ی upload حذف). endpoint `/api/imports` (staff)، صفحه‌ی `/staff/import`. ۴ تست حیاتی (scope isolation, below_committed, absent_but_committed, idempotency) + e2e (staff 201/agent 403/dedupe). **کلِ v1 حالا فیچرکامل است.**
- **مسیر backorder (محصول ناموجود، spec ۵.۶):** حواله‌ی مستقلِ backorder (بدون SalesRequest، `lot_id=NULL`) که **هیچ ردیف موجودی/لجری را دست نمی‌زند** — تعریفِ کلیدیِ backorder. چرخه‌ی `backorder_status` (pending_production→ready→fulfilled|cancelled) روی هر item، با guard. endpointها: POST `/api/sales-dispatches` (branch با `items`), POST `/api/backorders/:itemId/status`, GET `/api/backorders|agents|catalog`. پنل staff: فرم ثبت backorder + لیست با گذارِ وضعیت. ۴ تست جدید (۲۱/۲۱) + e2e (staff 201/agent 403/موجودی دست‌نخورده). زنجیره‌ی فروش حالا کامل: چه موجود چه ناموجود.
- **نقش‌محوری (role-based authz):** `tenant_membership.role` با CHECK (`admin`/`staff`/`agent`). تأیید تجاری و ساخت/بارگیریِ حواله حالا **staff-only** (`authorizeStaff`)؛ نماینده ۴۰۳ می‌گیرد. تأیید از صفحه‌ی نماینده به پنل staff منتقل شد (staff رزروهای active همه‌ی نماینده‌ها را می‌بیند و تأیید می‌کند). ۳ تست نقش (unit) + e2e گسترش‌یافته با دو نشست (نماینده/staff) که ۴۰۳/۲۰۱ را اثبات می‌کند.
- **UI کامل زنجیره:** صفحه‌ی `/reservations` (رزروهای من + دکمه‌ی تأیید) و `/staff` (درخواست‌های تأییدشده → ساخت حواله → گذارِ وضعیت تا loaded/delivered/cancel). read endpointها: `GET /api/reservations`, `GET /api/sales-requests`, `GET /api/sales-dispatches`. کل زنجیره از طریق HTTP روی سرور زنده تست شد (۱۴ چک، شامل تأییدِ کم‌شدنِ فیزیکیِ on_hand ۵۰→۴۰).
- **SalesDispatch + گذارِ `loaded`** (`dispatches.ts` + `/api/sales-dispatches` و `/:id/status`): ساخت حواله از SalesRequestِ تأییدشده، و بارگیریِ فیزیکی که `on_hand` و `allocated` را اتمیک کم می‌کند (spec ۱۴.۳). idempotent و state-guarded (بارگیریِ دوباره double-decrement نمی‌کند)؛ لغوِ قبل از بارگیری `allocated` را آزاد و request را cancelled می‌کند. حلقه‌ی آخرِ زنجیره‌ی موجودی. ۳ تست جدید (۱۴/۱۴ سبز).
- **الگوریتم Approval** (`salesRequests.ts` + `POST /api/reservations/:id/approve`): تبدیل رزرو → SalesRequest تأییدشده، جابه‌جاییِ اتمیکِ `held → allocated` بدون گپ زمانی (spec ۱۴.۲). قفل `ORDER BY lot_id`، guardِ `active`+منقضی‌نشده، ضدِ double-allocate. ۳ تست جدید (۱۱/۱۱ سبز).
- اسناد کنترلی MVP در `docs/`: PRD، ARCHITECTURE، API_SPEC، DATABASE_SCHEMA، CODING_STANDARDS، AI_CONTEXT، KNOWN_ISSUES + README ریشه و این CHANGELOG.
- اولین UI نماینده: `/login` و `/reserve` (RTL فارسی، pending-state، خطای ۴۰۹ «موجودی فعلی: X»، هشدار نرم مخلوط شید). — `83d908f`
- endpointها: `/api/auth/login`, `/api/me`, `/api/lots`, `/api/reservations`.
- پایه‌ی auth: bcryptjs + jose (JWT کوکی HttpOnly) + chokepoint دسترسی `authorizeAgent` (ضدِ IDOR). — `db1e281`
- اسکلت Next.js + Drizzle(introspect) + الگوریتم رزرو (`reservations.ts`). — `2ed87fe`
- DDL کامل Postgres (`db/schema.sql`) + تست دود (`db/test_schema.sql`). — `3c2f6d6`
- wireframe متنی UX نماینده. — `38de027`
- سند جامع محصول + CLAUDE.md؛ بخش ۷ با پیش‌فرض‌های تحقیق‌شده پر شد. — `8ad9bce`, `d41975a`

### Changed / Fixed
- **بازبینی چند-مدلی v7.1** (`5976f6d`): حذف `partially_converted` (ناسازگار با held)؛ idempotency به `UNIQUE(tenant_id,key)` + payload-hash؛ افزودن `sqcm_per_box`/`pieces_per_box`؛ `line_no` جای `UNIQUE(request_id,variant_id)`؛ CHECK قوی‌تر dispatch + FK سازگاریِ lot↔variant؛ FK `agent_account_user→tenant_membership`؛ `audit_log.old/new_value`؛ import scope. + بخش ۱۴ spec (الگوریتم approve/load/cancel، RLS عملیاتی، IRR/UTC، migration، زیرساخت ایران).
- کوکی نشست فقط در production `Secure` (در dev/http لاگین نمی‌شکند).
- cast `held`/`available` به int در VIEW (SUM بیگ‌اینت رشته برمی‌گرداند).

### تست‌ها (سبز در هر نقطه‌ی عطف)
schema smoke ۵/۵ · unit (reserve+authz) ۸/۸ · end-to-end ۸/۸ روی سرور زنده · `tsc` پاک · `next build` سبز.

---
> نسخه‌گذاری معنایی (SemVer) از اولین release عمومی شروع می‌شود. تا آن موقع همه‌چیز زیر `Unreleased`.
