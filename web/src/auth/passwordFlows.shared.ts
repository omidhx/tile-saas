/**
 * ثابت‌هایی که **هم** سرور و **هم** کلاینت لازم دارند.
 *
 * جدا از `passwordFlows.ts` چون آن فایل `@/db/client` را می‌آورد و import کردنش
 * در یک client component کلِ درایورِ postgres را وارد باندلِ مرورگر می‌کند.
 * سرور همین را re-export می‌کند تا یک مقدارِ واحد باقی بماند.
 */

/** حداقلِ طولِ رمز. کوتاه‌تر از این در برابر حدسِ آفلاین بی‌معنی است. */
export const MIN_PASSWORD = 8;

/**
 * حداکثرِ طولِ رمز. bcrypt بایتِ ۷۳ به بعد را بی‌صدا نادیده می‌گیرد — بدونِ سقف،
 * یک رشته‌ی چندمگابایتی هم قبل از آن truncation کامل هش می‌شود (هزینه‌ی CPU/حافظه
 * بابتِ چیزی که اصلاً در نتیجه اثر ندارد).
 */
export const MAX_PASSWORD = 72;
