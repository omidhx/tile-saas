# CLAUDE.md

راهنمای این پروژه برای Claude Code. مرجع کامل و استدلال‌ها در `tile-saas-comprehensive-spec.md` (ریشه‌ی پروژه) است — این فایل فقط چکیده‌ی چیزیه که باید رفتار کدنویسی رو عوض کنه.

## پروژه چیه

Vertical SaaS چندمستأجری برای کارخانه‌های کاشی/سرامیک ایران: موجودی + رزرو + درخواست سفارش نماینده‌ها، به‌جای تماس تلفنی و اکسل روزانه. زنجیره: مشاهده موجودی → رزرو → SalesRequest → تأیید → SalesDispatch → بارگیری.

**در MVP نیست:** حسابداری کامل، فاکتور قانونی، پرداخت آنلاین، لجستیک کامل، اتصال ERP. اگه یه فیچر داره به این‌ها نزدیک می‌شه، قبل از پیاده‌سازی بپرس.

## Stack و زیرساخت

- Next.js + TypeScript، Postgres (self-hosted)
- **میزبانی اپلیکیشن نهایی: VPS ایران** — چون مخاطب کاملاً داخلیه و در قطعی اینترنت بین‌الملل باید در دسترس بمونه (دلیل کامل در سند اصلی، بخش ۳). بک‌آپ رمزنگاری‌شده رو ترجیحاً روی یه مقصد خارجی هم نگه‌دار.
- **ولی موقع توسعه/دیپلوی: هرگز Vercel یا Supabase مدیریت‌شده استفاده نکن** — تحریم OFAC. خودت روی VPS با Docker/PM2 + Nginx/Caddy بالا بیار. Next.js و Postgres خودشون مشکلی ندارن (متن‌بازن)، فقط لایه‌ی سرویسِ مدیریت‌شده‌ی آمریکایی مشکل‌سازه.
- کد: mirror روی دو remote (نه فقط گیت‌هاب پرایوت)

## قوانین معماری غیرقابل‌نقض

این‌ها بعد از ۷ دور بازبینی مهندسی به این شکل رسیدن. هرکدوم رو که بشکنی، یه باگ قبلاً حل‌شده برمی‌گرده:

1. **`available = on_hand − held − allocated − blocked`.** `held` هیچ‌وقت ستون ذخیره‌شده نیست — همیشه با `SUM` روی رزروهای `active` و `expires_at > NOW()` محاسبه می‌شه. اگه یه‌جا وسوسه شدی یه فیلد کش‌شده برای held اضافه کنی، نکن — این دقیقاً همون باگی بود که چند نسخه طول کشید حلش کنیم.
2. **رزرو هرگز نباید موجودی رو منفی کنه.** `available ≥ requested` یه چک اجباریه، بدون استثنا. Backorder («ثبت محصول ناموجود») از مسیر کاملاً جدا (`SalesDispatchItem` با `lot_id = NULL` و `fulfillment_type = 'backorder'`) انجام می‌شه، هرگز با دور زدن این چک.
3. **رزرو چندآیتمی all-or-nothing است.** یا همه‌ی اقلام رزرو می‌شن یا هیچ‌کدوم — نصفه رزرو نکن.
4. **قفل رزرو همیشه با `ORDER BY lot_id`.** جلوی deadlock وقتی دو تراکنش چند Lot مشترک قفل می‌کنن. **ردیف `InventoryBalance` تنها mutex هر lot است** — هر مسیری که موجودی رو تغییر می‌ده (رزرو، تبدیل، Dispatch→loaded) باید همون ردیف رو `FOR UPDATE` قفل کنه، وگرنه تراکنش‌ها سریالایز نمی‌شن.
5. **`SalesRequest` ≠ `SalesDispatch`.** Request تأیید تجاریه (`held→allocated`). Dispatch اجرای فیزیکیه که staff (نه خود نماینده) می‌سازه؛ `on_hand` فقط وقتی کم می‌شه که Dispatch به `loaded` بره — و این باید تو یه تراکنش دیتابیسی واحد با ثبت لجر باشه، نه دو عملیات جدا.
6. **همه‌جا `tenant_id` + FK ترکیبی.** هر جدول دامنه‌ای `tenant_id` داره؛ روابط بین جدول‌ها با `FOREIGN KEY (tenant_id, x_id) REFERENCES x(tenant_id, id)` تعریف می‌شن (نیازمند `UNIQUE(tenant_id, id)` روی مقصد)، به‌علاوه‌ی RLS به‌عنوان لایه‌ی دوم دفاعی. یکی بدون اون یکی کافی نیست.
7. **متراژ و پول هیچ‌وقت float نیست.** کوچیک‌ترین واحد صحیح (سانتی‌متر مربع، کوچیک‌ترین واحد پولی)؛ اعشار فقط در لایه‌ی UI.
8. **Authorization > Authentication.** بزرگ‌ترین ریسک این اپ IDOR و نشت بین‌تننتیه، نه SQL injection. هر endpoint باید چک کنه: کاربر عضو این Tenant هست؟ نقشش این کار رو مجاز می‌کنه؟ رکورد واقعاً مال همینه؟

## چیزهایی که امتحان شد و رد شد — دوباره پیشنهادشون نده

- ❌ `total_qty` + `reserved_qty` به‌عنوان دو ستون جدا روی موجودی — تناقض با محاسبه‌ی lazy ایجاد می‌کنه (به‌جاش: بند ۱ بالا)
- ❌ Refetch صریح قبل از submit رزرو — کندتره و race condition رو حل نمی‌کنه. مستقیم POST بزن، خطای `409 Conflict` رو در `onError` مدیریت کن.
- ❌ Optimistic UI برای رزرو (نشون‌دادن موفقیت قبل از تأیید سرور) — به‌جاش یه pending state ساده (دکمه غیرفعال + spinner)
- ❌ `is_backorder` به‌صورت boolean ساده — کافی نیست؛ به‌جاش `fulfillment_type` (in_stock/backorder) + `backorder_status`
- ❌ Redis / Optimistic Concurrency Control برای race condition — `SELECT FOR UPDATE` در تراکنش کوتاه برای این مقیاس (چند ده نماینده) کافیه
- ❌ Event Sourcing / CQRS کامل، یا Prometheus/Grafana رسمی — over-engineering برای این مرحله
- ❌ آرشیو/پارتیشن رزروهای منقضی یا retention policy زودهنگام — برای مقیاس این پروژه (چند ده نماینده) over-engineeringه؛ ایندکس جزئیِ `WHERE status='active'` کوئری held رو پوشش می‌ده. trigger واقعی برای بازبینی: اگه روزی هزاران رزرو فعالِ هم‌زمان شد (v2). چند بازبینِ AI این رو پیشنهاد دادن چون «۱۰۰۰ نماینده» فرض کردن که مقیاس این پروژه نیست.
- ❌ رزرو یا سفارش در حالت آفلاین — PWA فقط برای مشاهده‌ی کش‌شده، نوشتن همیشه آنلاین
- ❌ فیلد مالی cache‌شده (مثل «مانده‌ی حساب») بدون یه لجر مالی پشتش

## قراردادهای نام‌گذاری

- نام موجودیت فیزیکی همیشه `lot_id` (نه `inventory_lot_id`) — یکدستی در کل دیتابیس
- سفارش تجاری = `SalesRequest`، هرگز `Invoice` (بار حقوقی متفاوت)
- شناسه‌ها UUID

## چیزهایی که هنوز معلق‌ان — فرض نکن، بپرس

این‌ها نیاز به مصاحبه‌ی واقعی با کارخونه دارن. سند اصلی (بخش ۷) برای هرکدوم یه **پیش‌فرض پیشنهادیِ تحقیق‌شده** داره — باهاش پیش برو، ولی هاردکدش نکن؛ جواب کارخونه فقط تأیید/override می‌کنه:
- `shade_code`/`caliber_code` روی `InventoryLot` nullable بمونن، پشت `track_shade_caliber`. **پیش‌فرض config = `optional`** (نه off) — شید/کالیبر استاندارد بنیادیِ صنعت‌ان و رزرو باید بتونه به «همون شید+کالیبر» قید بخوره. مخلوط شید در یک سفارش: هشدار نرم، نه منع سخت.
- Import: **پیش‌فرض `import_mode = 'snapshot'`** (امن‌تر برای اکسل دستیِ روزانه؛ یه روز جاافتاده drift نمی‌سازه). Delta فقط اگه کارخونه صراحتاً گفت فایلش «فقط تغییرات» است.
- چند Lot در یک سفارش: **بله، مجازه** (FIFO روی `entry_date`، مقیدِ شید+کالیبر). واحد پایه = کارتن، پالت مشتق.
- `brand` روی `Product` بمونه (nullable، آماده‌ی مهاجرت به Lot اگه بچ‌ها relabel می‌شن).
- TTL رزرو: پیش‌فرض ۲۴ ساعت. مشتری نهایی: متن آزاد در MVP (نه Entity).

## ساختار و دستورها

- `db/schema.sql` — اسکیمای Postgres، **منبع حقیقت**. RLS/composite FK اینجاست، نه در ORM.
- `db/test_schema.sql` — تست دود schema (روی Postgres واقعی سبز).
- `web/` — اپ Next.js (App Router, TS, src-dir) + Drizzle (introspect، نه بازتعریف).
- `web/src/db/client.ts` — `withTenant()` که `app.tenant_id` را SET می‌کنه (RLS فعال).
- `web/src/db/reservations.ts` — **الگوریتم رزرو**، پیاده‌سازی مرجعِ بخش ۶ spec.
- `docs/ux-wireframes.md` — قرارداد رفتاریِ صفحه‌های نماینده.

```bash
# schema روی یه Postgres محلی/تازه (نصب مرجع). هرگز روی prodِ داده‌دار دوباره اجرا نکن —
# تغییرات prod فقط با migration نسخه‌دار در db/migrations/ (بخش ۱۴.۹ spec).
psql "$DATABASE_URL" -f db/schema.sql

cd web
npm run dev        # سرور توسعه
npm run build      # بیلد پروडاکشن
npm test           # تست یکپارچه‌ی رزرو (نیازمند DATABASE_URL به Postgres تازه)
npm run db:pull    # introspect اسکیمای typed از دیتابیس → src/db/generated/
```

اپ باید با نقشِ **non-superuser** به Postgres وصل شه، وگرنه RLS بایپس می‌شه.
`DATABASE_URL` در `web/.env` (gitignored)؛ نمونه در `web/.env.example`.

## مرجع کامل

جزئیات کامل مدل داده، الگوریتم رزرو، تصمیم‌های امنیتی، و استدلال پشت هرکدوم: `tile-saas-comprehensive-spec.md` (ریشه‌ی پروژه)
